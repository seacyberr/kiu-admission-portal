# =============================================================================
# KIU Admission Portal — Flask API dependencies
# =============================================================================
# Python: 3.11 or newer (test with: python --version)
# Works on Windows, macOS, and Linux.
#
# Install (from this directory):
#   pip install -U pip
#   pip install -r requirements.txt
#
# Recommended: use a virtual environment first (see README.md).
# =============================================================================

# --- Core Flask ---
Flask>=3.0.3,<4
Flask-SQLAlchemy>=3.1.1,<4
Flask-Cors>=5.0.0,<7
Flask-Bcrypt>=1.0.1,<2
Flask-JWT-Extended>=4.6.0,<5
Flask-Limiter>=3.5.0,<4
Flask-Caching>=2.1.0,<3
Werkzeug>=3.0.3,<4

# --- Database ---
SQLAlchemy>=2.0.36,<3
PyMySQL>=1.1.1,<2
alembic>=1.14.0,<2

# --- Caching / rate limiting backends ---
redis>=5.0.1,<6
limits>=3.13.0,<4

# --- Security & validation ---
cryptography>=44.0.0,<47
bcrypt>=4.2.1,<5
PyJWT>=2.10.0,<3
email-validator>=2.2.0,<3

# --- Serialization / validation ---
marshmallow>=3.23.1,<4
marshmallow-sqlalchemy>=1.1.0,<2
jsonschema>=4.26.0,<5

# --- Observability ---
sentry-sdk[flask]>=2.19.0,<3
structlog>=24.4.0,<26
prometheus-client>=0.19.0,<1
blinker>=1.7.0,<2

# --- Configuration & HTTP ---
python-dotenv>=1.0.1,<2
requests>=2.32.3,<3
urllib3>=2.2.3,<3

# --- Utilities ---
click>=8.1.7,<9
phonenumbers>=8.13.51,<10
python-dateutil>=2.9.0,<3

# --- Testing ---
pytest>=8.3.4,<10

# --- Production WSGI servers (markers avoid broken installs per OS) ---
gunicorn>=23.0.0,<26; sys_platform != "win32"
waitress>=3.0.2,<4; sys_platform == "win32"

# =============================================================================
# Optional (not installed by default — add if you need them)
# =============================================================================
# Load / stress testing:
#   pip install "locust>=2.24.0,<3"
# Coverage for pytest:
#   pip install "pytest-cov>=5.0.0,<7"
